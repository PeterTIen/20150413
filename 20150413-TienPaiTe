#include <stdio.h>
#include <string.h>

int main(){

  int i;
  char a[]="Fizz";
  char b[]="Buzz";
  
  for(i=1;i<=100;i++)
    {

	if(i%15==0)
	{
	   printf("%s%s\n", a ,b);
	}
	else if(i%5==0)
	{
	  printf("%s\n",b);
	}
	else if(i%3==0)
	{
	  printf("%s\n",a);
	} 
	else
	{
      	  printf("%d\n",i);
        }
    
    }

  return 0;

}
